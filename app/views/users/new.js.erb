// Add the dialog title
$('#modal-users-title').html("New user");

// Render the new form
$('.modal-body-users').html('<%= j render("form") %>');

// Show the dynamic dialog
$('#modalUsers').modal("show");